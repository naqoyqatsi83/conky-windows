name: Build Windows Installer

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup MSYS2
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          install: >-
            mingw-w64-x86_64-gcc
            mingw-w64-x86_64-cmake
            mingw-w64-x86_64-make
            mingw-w64-x86_64-lua
            mingw-w64-x86_64-git
            mingw-w64-x86_64-gperf

      - name: CMake Configure
        shell: msys2 {0}
        run: |
          cmake -B build -G "MinGW Makefiles" -DBUILD_NVIDIA_NVML=ON \
            -DLUA_INCLUDE_DIR=/mingw64/include \
            -DLUA_LIBRARY=/mingw64/lib/liblua.dll.a

      - name: Build
        shell: msys2 {0}
        continue-on-error: true
        run: |
          mingw32-make.exe -j$(nproc) -C build 2>&1 || true
          echo "=== Include rsp ==="
          cat build/src/CMakeFiles/conky.dir/includes_CXX.rsp 2>/dev/null || echo "No rsp"
          echo "=== Find generated ==="
          find build -name "defconfig.h" 2>/dev/null
          find build -name "convertconf.h" 2>/dev/null
          echo "=== Binary dir ==="
          cmake -LA build 2>/dev/null | grep CMAKE_BINARY_DIR
          echo "=== Current dir ==="
          pwd
